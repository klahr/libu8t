image: archlinux
packages:
  - valgrind
  - cmake
sources:
  - https://git.sr.ht/~klahr/libu8t
  - https://git.sr.ht/~klahr/unit-check
tasks:
  - setup: |
      cd unit-check
      mkdir -p build
      cd build
      cmake ..
      make
      sudo make install
  - build: |
      cd libu8t
      make
  - test: |
      cd libu8t
      cd tests
      make
      valgrind --leak-check=full ./tests

